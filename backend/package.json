{
  "name": "skybound-aviation-backend",
  "version": "1.0.0",
  "description": "Backend para Skybound Aviation con Node.js, Express y PostgreSQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node scripts/init-db.js",
    "init-data": "node scripts/init-data.js",
    "setup": "npm run init-db && npm run init-data",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "express",
    "postgresql",
    "neon",
    "aviation"
  ],
  "author": "Skybound Aviation",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "pg": "^8.11.3",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
} 